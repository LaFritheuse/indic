name: Diagnose exchanges (temporary)

on:
  workflow_dispatch: {}  # manuel uniquement, pas de cron -- outil de diagnostic ponctuel

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install ccxt
      - run: python data_collection/diagnose_exchanges.py
